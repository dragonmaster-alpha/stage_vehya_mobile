import 'package:flutter/material.dart';

class Licensing extends StatelessWidget {
  const Licensing({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
